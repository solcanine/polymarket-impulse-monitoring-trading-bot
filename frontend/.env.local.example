# Same URI and DB name as the root bot (.env) so the dashboard reads/writes shared state.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=polymarket_impulse
# Optional; matches backend default (enabled unless set to "false").
# ENABLE_IMPULSE_BOT=true
